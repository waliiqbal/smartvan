export enum GameAnswer {
  STRAIGHT = 'straight',
  LEFT = 'left',
  RIGHT = 'right',
}

export enum DriverPosition {
  LEFT_HAND = 'lefthand',
  RIGHT_HAND = 'righthand',
}

export enum ActionResult {
  WIN = 'Win',
  LOSE = 'Lose',
  TIE = 'Tie',
  DIE = 'Die',
}
